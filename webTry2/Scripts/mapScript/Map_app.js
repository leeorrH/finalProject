var app = angular.module("googleMap", []);
app.controller("mapController", function ($scope, $http, $location) {
    /*
     * main controller functions:
     * init map 
     * insert markers (include info window)
     * remove markers by user requierments
     * cluster marks 
     * show encryptor location history
     * insert bulding marks + creating window info that show all encryptors inside the building
     * 
     * */
}